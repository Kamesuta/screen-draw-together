<Page x:Class="ScreenDrawTogether.Pages.Guest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="450"
      Title="Guest" Unloaded="Page_Unloaded">

    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <Label Content="参加" FontSize="36" HorizontalAlignment="Center"/>
            <Grid Height="300" Margin="46,0,46,0">
                <StackPanel VerticalAlignment="Center">
                    <Label Content="QRコードを含む画面をDiscordなどで画面してもらってください" HorizontalAlignment="Left"/>
                    <Separator Opacity="0" Height="20"/>
                    <Button x:Name="GuestButton" Content="参加する" Height="60" Click="GuestButton_Click"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
